# To build & execute lib tests:

```console
sudo cargo test -- --nocapture
```

**NOTE:** you would need to build & install the `libxdpiface` .so before building
& running the rust bindings lib & tests.
